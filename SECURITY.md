# Security

Report security issues privately to the repository owner rather than opening a public issue. MockRoute contains no account system or telemetry. Never commit signing keys, passwords, API secrets, or `local.properties`.

MockRoute is a transparent test-provider tool. Requests to conceal mocked locations, bypass platform mock markers, or evade app detection are out of scope.
